{
  "name": "miEAA3_mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for miEAA enrichment analysis",
  "main": "dist/server.js",
  "bin": {
    "mieaa-mcp-server": "dist/server.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "clean": "rimraf dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "chokidar": "^3.5.0",
    "node-fetch": "^3.3.2",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.17.5",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.0.0"
  }
}
